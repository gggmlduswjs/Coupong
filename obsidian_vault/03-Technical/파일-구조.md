# 파일 구조

#technical #structure #files

**작성일:** 2026-02-05

---

## 📁 전체 디렉토리 구조

```
C:\Users\MSI\Desktop\Coupong\
│
├── app/                              애플리케이션 코어
│   ├── models/                       데이터 모델 (SQLAlchemy)
│   │   ├── __init__.py              모델 export
│   │   ├── account.py               계정 모델
│   │   ├── publisher.py             출판사 모델 + 마진 계산
│   │   ├── book.py                  도서 모델 + 연도 추출
│   │   ├── product.py               단권 상품 모델
│   │   ├── bundle_sku.py            묶음 상품 모델
│   │   ├── listing.py               업로드 현황 (중복 체크)
│   │   ├── sales.py                 판매 데이터
│   │   └── analysis_result.py       분석 결과
│   │
│   ├── config.py                     설정 (Pydantic Settings)
│   └── database.py                   DB 연결 (SQLAlchemy Engine)
│
├── crawlers/                         데이터 수집
│   ├── __init__.py
│   ├── aladin_api_crawler.py        알라딘 API 크롤러
│   └── base_crawler.py              (선택) 크롤러 베이스 클래스
│
├── analyzers/                        데이터 분석
│   ├── __init__.py
│   ├── margin_calculator.py         마진 계산기
│   └── bundle_generator.py          묶음 SKU 생성기
│
├── exporters/                        데이터 출력
│   ├── __init__.py
│   └── csv_generator.py             CSV 생성 (쿠팡 포맷)
│
├── scripts/                          실행 스크립트
│   ├── init_db_v2_clean.py          DB 초기화
│   ├── smart_upload_system.py       전체 워크플로우
│   ├── example_workflow.py          워크플로우 예시
│   └── example_with_obsidian_logging.py  Obsidian 로깅 예시
│
├── config/                           설정 파일
│   └── publishers.py                24개 출판사 데이터
│
├── utils/                            유틸리티 (선택)
│   ├── __init__.py
│   ├── logger.py                    로깅 유틸
│   └── validators.py                검증 함수
│
├── tests/                            테스트 (선택)
│   ├── __init__.py
│   ├── test_models.py               모델 테스트
│   ├── test_analyzers.py            분석기 테스트
│   └── test_integration.py          통합 테스트
│
├── obsidian_vault/                   Obsidian 문서
│   ├── 00-Index/                    인덱스 및 개요
│   │   ├── Index.md                 메인 대시보드
│   │   ├── 프로젝트-개요.md         프로젝트 전체 개요
│   │   ├── Development-Timeline.md  개발 타임라인
│   │   └── Claude-Integration.md    Claude 통합 가이드
│   │
│   ├── 01-Daily/                    일일 개발 로그
│   │   └── 2026-02-05.md           오늘의 로그
│   │
│   ├── 02-Features/                 기능 문서
│   │   ├── Database-Schema-V2.md    DB 스키마
│   │   ├── 마진-계산기.md           마진 계산 기능
│   │   ├── 연도-추출.md             연도 추출 기능
│   │   ├── 묶음-SKU-생성기.md       묶음 생성 기능
│   │   └── 계정별-판매-통계.md      (예시) 통계 기능
│   │
│   ├── 03-Technical/                기술 문서
│   │   ├── Tech-Stack.md            기술 스택
│   │   ├── 시스템-아키텍처.md       시스템 구조
│   │   ├── 사용-가이드.md           사용법
│   │   ├── 설정-가이드.md           설정 방법
│   │   ├── 파일-구조.md             (이 문서)
│   │   └── 계정별-통계-쿼리.md      SQL 쿼리 예시
│   │
│   ├── 04-Decisions/                의사결정 기록
│   │   └── 통계-계산-방식.md        결정 사항 기록
│   │
│   ├── 05-Bugs/                     (선택) 버그 기록
│   │
│   └── README.md                    Vault 소개
│
├── output/                           출력 파일
│   ├── coupang_upload_account_1.csv
│   ├── coupang_upload_account_2.csv
│   ├── coupang_upload_account_3.csv
│   ├── coupang_upload_account_4.csv
│   └── coupang_upload_account_5.csv
│
├── logs/                             로그 파일 (선택)
│   ├── app.log
│   ├── crawler.log
│   └── error.log
│
├── backups/                          백업 (선택)
│   └── coupang_backup_20260205.db
│
├── data/                             데이터 파일 (선택)
│   ├── raw/                         원본 데이터
│   └── processed/                   가공 데이터
│
├── obsidian_logger.py                Obsidian 자동 로깅 클래스
├── example_with_obsidian_logging.py  로깅 예시 스크립트
│
├── coupang.db                        SQLite 데이터베이스
├── .env                              환경 변수 (Git 제외)
├── .gitignore                        Git 제외 파일 목록
├── requirements.txt                  Python 패키지 목록
├── README.md                         프로젝트 소개
│
├── DATABASE_SCHEMA_V2.md             DB 스키마 문서
└── OBSIDIAN_CLAUDE_INTEGRATION.md   Claude 통합 상세 가이드
```

---

## 📄 주요 파일 설명

### 핵심 모델 (app/models/)

#### publisher.py
```python
class Publisher(Base):
    """출판사 모델 - 마진 계산 로직 포함"""

    # 필드
    - name: 출판사명
    - margin_rate: 매입율 (40-73%)
    - supply_rate: 공급률 (0.27-0.60)
    - min_free_shipping: 무료배송 기준

    # 메서드
    - calculate_margin(): 마진 계산
    - determine_shipping_policy(): 배송 정책 결정
```

#### book.py
```python
class Book(Base):
    """도서 모델 - 연도 추출 로직 포함"""

    # 필드
    - isbn, title, author, publisher_id
    - list_price: 정가
    - year: 추출된 연도
    - normalized_title: 정규화된 제목
    - normalized_series: 시리즈명

    # Static 메서드
    - extract_year(): 제목에서 연도 추출
    - normalize_title(): 제목 정규화
    - extract_series(): 시리즈명 추출
```

#### product.py
```python
class Product(Base):
    """단권 상품 모델 - 마진 정보 포함"""

    # 필드
    - book_id, isbn
    - sale_price: 판매가 (정가 × 0.9)
    - supply_rate, margin_per_unit, net_margin
    - shipping_policy: 배송 정책
    - can_upload_single: 단권 업로드 가능 여부

    # Class 메서드
    - create_from_book(): Book에서 Product 생성
```

#### bundle_sku.py
```python
class BundleSKU(Base):
    """묶음 상품 모델"""

    # 필드
    - bundle_key: 묶음 식별자
    - publisher_id, normalized_series, year
    - book_count, book_ids, isbns
    - total_sale_price, net_margin

    # Class 메서드
    - create_bundle(): 도서 목록에서 묶음 생성
```

#### listing.py
```python
class Listing(Base):
    """업로드 현황 - 중복 체크"""

    # 제약조건
    - UNIQUE(account_id, isbn)         단권 중복 방지
    - UNIQUE(account_id, bundle_key)   묶음 중복 방지

    # 필드
    - account_id
    - product_type: 'single' | 'bundle'
    - product_id, bundle_id
    - isbn, bundle_key
```

---

### 크롤러 (crawlers/)

#### aladin_api_crawler.py
```python
class AladinAPICrawler:
    """알라딘 API 크롤러"""

    # 메서드
    - search_books(query, max_results): 도서 검색
    - _parse_item(item): JSON 파싱 + 연도 추출
```

**주요 기능:**
- 알라딘 Open API 호출
- 연도 자동 추출
- 시리즈 정규화
- JSON → Book 모델 변환

---

### 분석기 (analyzers/)

#### margin_calculator.py
```python
class MarginCalculator:
    """마진 분석기"""

    # 메서드
    - analyze_book(book, publisher): 도서 마진 분석
    - analyze_all_books(): 전체 도서 분석
    - filter_profitable(books, min_margin): 수익성 필터
```

**계산 로직:**
```
순마진 = (정가×0.9 - 정가×공급률 - 판매가×0.11) - 2000
```

#### bundle_generator.py
```python
class BundleGenerator:
    """묶음 SKU 생성기"""

    # 메서드
    - find_bundleable_books(): 묶음 대상 찾기
    - auto_generate_bundles(min_margin): 자동 묶음 생성
```

**생성 조건:**
- 같은 출판사 + 시리즈 + 연도
- 최소 2권, 최대 10권
- 순마진 >= 설정 값

---

### 출력기 (exporters/)

#### csv_generator.py
```python
class CSVGenerator:
    """CSV 생성기 (쿠팡 포맷)"""

    # 메서드
    - generate_account_csv(account_id): 계정별 CSV
    - generate_all_account_csvs(): 전체 계정 CSV
```

**CSV 포맷:**
```csv
ISBN,제목,판매가,배송비,배송정책
9788...,수능완성 국어,7200,0,무료배송
```

---

### 스크립트 (scripts/)

#### init_db_v2_clean.py
```python
"""DB 초기화 스크립트"""

# 수행 작업
1. 테이블 생성 (8개)
2. 출판사 데이터 등록 (24개)
3. 테스트 계정 생성 (5개)
4. 마진 계산 테스트
```

#### smart_upload_system.py
```python
"""전체 워크플로우 실행"""

class SmartUploadSystem:
    def run_full_workflow():
        # 1. 알라딘 검색
        # 2. 마진 분석
        # 3. 묶음 생성
        # 4. 계정 분산
        # 5. CSV 생성
```

---

### 설정 (config/)

#### publishers.py
```python
"""24개 출판사 데이터"""

PUBLISHERS = {
    "EBS": {
        "margin_rate": 73,
        "supply_rate": 0.27,
        "min_free_shipping": 50800
    },
    # ... 23개 더
}
```

---

### Obsidian 로거

#### obsidian_logger.py
```python
class ObsidianLogger:
    """Obsidian 자동 로깅"""

    # 메서드
    - log_to_daily(content, title): 일일 노트에 추가
    - log_feature(name, description, status): 기능 문서 생성
    - log_technical(name, content): 기술 문서 생성
    - log_decision(title, context, decision): 결정 사항 기록
    - log_bug(title, symptom, solution): 버그 기록
```

---

## 🔧 환경 파일

### .env (환경 변수)
```bash
# 알라딘 API
ALADIN_TTB_KEY=your_ttb_key_here

# 데이터베이스
DATABASE_URL=sqlite:///coupang.db

# Obsidian
OBSIDIAN_VAULT_PATH=C:\Users\MSI\Desktop\Coupong\obsidian_vault

# 계정
DEFAULT_DAILY_UPLOAD_LIMIT=20
NUM_ACCOUNTS=5
```

### .gitignore
```
# 환경 변수
.env

# 데이터베이스
*.db
*.db-journal

# 출력 파일
output/
logs/
backups/

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/

# IDE
.vscode/
.idea/
*.swp
```

### requirements.txt
```
SQLAlchemy==2.0.23
pydantic==2.5.0
pydantic-settings==2.1.0
requests==2.31.0
python-dotenv==1.0.0
```

---

## 📦 데이터베이스

### coupang.db (SQLite)
```
8개 테이블:
- accounts          5개 계정
- publishers        24개 출판사
- books             알라딘 도서 데이터
- products          단권 상품
- bundle_skus       묶음 상품
- listings          업로드 현황
- sales             판매 데이터
- analysis_results  분석 결과
```

---

## 📤 출력 파일

### CSV 파일 (output/)
```
coupang_upload_account_1.csv
coupang_upload_account_2.csv
coupang_upload_account_3.csv
coupang_upload_account_4.csv
coupang_upload_account_5.csv
```

**포맷:**
- UTF-8 with BOM (Excel 호환)
- 쿠팡 업로드 포맷
- 계정별 중복 제외

---

## 🔍 파일별 의존성

### 의존성 그래프

```
config.py
  ↑
database.py
  ↑
models/*.py
  ↑
├─ crawlers/aladin_api_crawler.py
├─ analyzers/margin_calculator.py
├─ analyzers/bundle_generator.py
└─ exporters/csv_generator.py
  ↑
scripts/smart_upload_system.py
```

### Import 예시

```python
# 표준 import 순서
from app.config import settings
from app.database import get_db, engine
from app.models import Publisher, Book, Product, BundleSKU
from crawlers.aladin_api_crawler import AladinAPICrawler
from analyzers.margin_calculator import MarginCalculator
from analyzers.bundle_generator import BundleGenerator
from exporters.csv_generator import CSVGenerator
```

---

## 📝 네이밍 규칙

### 파일명
```
- Python: snake_case
  예) margin_calculator.py, bundle_generator.py

- Markdown: Kebab-Case 또는 한글
  예) 프로젝트-개요.md, 사용-가이드.md
```

### 클래스명
```python
- PascalCase
  예) MarginCalculator, BundleGenerator, ObsidianLogger
```

### 함수/변수명
```python
- snake_case
  예) calculate_margin(), generate_bundle(), net_margin
```

### 상수명
```python
- UPPER_SNAKE_CASE
  예) MIN_MARGIN, MAX_BOOKS_PER_BUNDLE, DEFAULT_SHIPPING_COST
```

---

## 🔗 관련 문서

- [[프로젝트-개요]] - 프로젝트 전체 개요
- [[시스템-아키텍처]] - 시스템 구조
- [[Tech-Stack]] - 기술 스택
- [[사용-가이드]] - 사용법

---

**작성:** 2026-02-05
**상태:** 문서화 완료
